const city = "pune";
city = "mumbai";
console.log(city);

// - if a variable is declared with const keyword then
//   initialization is must
// - value of a variable declared with const keyword cannot be changed
